# Firestore Emulator Server Configuration

# Server host (default: localhost)
HOST=localhost

# Server port (default: 3333)
PORT=3333

# Default project ID (default: demo-project)
PROJECT_ID=demo-project
