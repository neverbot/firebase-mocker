/**
 * Firestore emulator: gRPC server and in-memory storage compatible with
 * Firebase Admin/Firestore when FIRESTORE_EMULATOR_HOST is set.
 */

export { FirestoreServer } from './server';
export { Storage } from './storage';
